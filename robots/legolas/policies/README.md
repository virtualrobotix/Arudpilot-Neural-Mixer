# Policy per Legolas

Solo policy per `legolas` (10 giunti, osservazione 39). Sulla microSD: `/APM/nnm/legolas/policies/`.

Nessuna policy ancora. Addestrare con `tools/robots/train_velocity.py --robot legolas` (vedi docs/robots/legolas.md).
