# Policy per AlbertPro

Solo policy per `albert` (8 giunti, osservazione 33). Sulla microSD: `/APM/nnm/albert/policies/`.

Nessuna policy ancora. Addestrare con `tools/robots/train_velocity.py --robot albert` (vedi docs/robots/albert.md).
