# Policy per Rex / SpotMicro

Solo policy per `rex` (12 giunti, osservazione 45). Sulla microSD: `/APM/nnm/rex/policies/`.

Nessuna policy ancora. Addestrare con `tools/robots/train_velocity.py --robot rex` (vedi docs/robots/rex.md).
