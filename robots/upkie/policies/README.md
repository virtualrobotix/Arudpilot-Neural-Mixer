# Policy per Upkie (wheeled biped)

Solo policy per `upkie` (6 giunti, osservazione 27). Sulla microSD: `/APM/nnm/upkie/policies/`.

Nessuna policy ancora. Addestrare con `tools/robots/train_velocity.py --robot upkie` (vedi docs/robots/upkie.md).
